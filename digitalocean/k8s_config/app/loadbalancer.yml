---
apiVersion: v1
kind: Service
metadata:
  name: emojify-ingress-service
spec:
  selector:
    app: emojify-ingress
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer
